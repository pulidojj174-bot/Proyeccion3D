export interface StatData {
  id: string;
  icon: string;
  value: string;
  label: string;
  delay?: number;
}

export interface ValueData {
  id: string;
  title: string;
  description: string;
  delay?: number;
}
